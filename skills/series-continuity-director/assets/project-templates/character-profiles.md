# {{SERIES_TITLE}} - Character Profiles
series_id: {{SERIES_ID}}
updated_at: {{UPDATED_AT}}

## Character records

### C01 - [name]

#### Stable identity pointers
- Species Morphology Profile ID:
- Species Morphology Profile SHA-256:
- Individual Morphology Contract ID:
- Individual Morphology Contract SHA-256:
- Character Identity Contract ID:
- Character Identity Contract SHA-256:
- current Era Contract:
- current Form Contract:
- current Appearance Variant Contract:
- approved reference sets:
- external visual-contract envelope ID and SHA-256:
- originating capability-manifest SHA-256:
- contract profile and required features:
- optional features and interpretation status:
- frame-character reference ID and SHA-256, when supplied:
- part-measurement reference IDs and SHA-256 values, when supplied:
- accessory-geometry reference IDs and SHA-256 values, when supplied:
- visual-authority or visual-evidence bundle ID and SHA-256, when supplied:

#### Species and body-plan differential
- canonical species, lineage, chassis, or original creature name:
- reality status and origin:
- body-region graph and locomotion class:
- meaningful present features and counts:
- meaningful absent or inapplicable features:
- attachment topology for limbs, tail, wings, fins, tentacles, horns, sensors, and special organs:
- surface systems and material boundaries:
- expression and communication channels:
- near-species or near-design differences:
- developmental, polymorphic, seasonal, or form possibilities:
- unresolved species questions:

#### Approved identity summary
- life stage, active baseline form, and scale:
- exact proportions and primary measurements:
- stable feature instances and laterality:
- face, hair, fur, skin, scales, feathers, shell, casing, or display surfaces:
- eyes, optics, nose, muzzle, beak, mouth, ears, sensors, and sensory structures:
- hands, paws, hooves, fins, manipulators, digit count, nails, claws, talons, pads, or terminal plates:
- tail, wing, fin, tentacle, horn, antenna, crest, gill, pouch, luminous organ, tool mount, or other special structures:
- stable markings, scars, tattoos, notches, wear, repairs, and asymmetries:
- grooming, facial hair, brows, lashes, body hair, manicure, and other identity-linked presentation:
- personal expression behavior and external expression tools:
- individual differential from close characters or variants:
- approved variable fields and mutable state paths:
- unresolved individual questions:

#### Visual proof and coverage
Each line names the accepted sheet panel that proves it, by slot id from the `reference-bundle-plan`.
- turnaround views that prove body topology and attachment sites:
- top and underside views that prove the outline from above and below:
- views that prove head and sensory structure:
- views that prove hands, feet, digits, claws, or manipulators:
- views that prove markings, surfaces, grooming, and asymmetries:
- views that prove expression channels and tools:
- reference bundle ID and SHA-256:
- unsupported or weakly supported features:

#### Physical performance vocabulary
The sections mirror the Character Sheet's performance tables. Name each entry the way its sheet row is named, so a shot can ask for it by name and attach the panel that proves it.
- default posture and weight distribution (`bodylang` baseline row):
- expression states (`expressions` rows, one per named state):
- body-language states (`bodylang` rows, one per named state):
- part states (`partstates` rows: the tail, ears, crest, or other part in each named configuration):
- range of motion (`motion` rows: each region, its extremes, and the limit it does not pass):
- action poses (`actions` rows: each action and phase):
- everyday gestures (`gestures` rows: each habit, its context, and its props):
- gaze and head timing:
- stress signals (named by the rows above that carry them):
- relief, affection, anger, fear, attraction, rivalry, and concealment signals (named by the rows above that carry them):
- signature movements:
- signals that require an external object, tool, screen, badge, instrument, scent, sound, light, or fluid:

#### Voice and language
- canonical spoken language:
- voice qualities:
- thinking grammar:
- vocabulary domain:
- sentence fingerprint:
- subtitle policy:

#### Relationship behavior
- public behavior:
- private behavior:
- contact boundaries:

#### Out-of-character boundaries
- none approved

#### Proposed traits
- none
