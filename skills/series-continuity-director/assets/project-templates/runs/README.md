# Run packages

Each run directory preserves the exact submission sheet, literal field contents, submitted media pointers, settings, every returned variant, direct observations, finishing record, and accepted endpoint.
